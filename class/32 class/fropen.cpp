#include <iostream>
#include <string>
#include <vector>
#include <queue>
using namespace std;

int main()
{
    // freopen("input.txt", "r", stdin); // SDL 꺼야 실행
    // freopen_s(new FILE*,"input.txt", "r", stdin);// SDL 안 꺼도 실행
    int n;
    cin >> n;

    double arr[10];
    for (int i = 0; i < n; i++)
    {
        cin >> arr[i];
    }
}